@extends('layouts.auth') @section('title','設定新密碼') @section('content')<h1>設定新密碼</h1><form method="post" action="{{ route('password.update') }}" class="admin-form">@csrf<input type="hidden" name="token" value="{{ $token }}"><label>Email<input type="email" name="email" value="{{ old('email',$email) }}" required></label><label>新密碼<input type="password" name="password" required></label><label>確認新密碼<input type="password" name="password_confirmation" required></label>@foreach($errors->all() as $error)<p class="field-error">{{ $error }}</p>@endforeach<button class="admin-button" type="submit">更新密碼</button></form>@endsection
