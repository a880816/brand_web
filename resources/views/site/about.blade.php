@extends('layouts.site') @section('title',$page->seo_title ?: $page->title.'｜'.$brand->name) @section('content')<section class="page-hero"><p class="eyebrow">ABOUT</p><h1>{{ $page->title }}</h1><p>{{ $page->excerpt }}</p></section><section class="detail-grid section"><div class="prose">@foreach(preg_split('/\n\n+/', $page->body) as $p)<p>{{ $p }}</p>@endforeach</div><x-media-image :media="$page->primaryMedia('hero_desktop')" ratio="3/4" /></section><section class="section"><x-gallery :media="$page->mediaFor('gallery')->get()" /></section>@endsection
