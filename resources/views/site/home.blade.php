@extends('layouts.site') @section('content')
<section class="hero"><picture>@php($desktop=$brand->primaryMedia('hero_desktop'))@php($mobile=$brand->primaryMedia('hero_mobile'))@if($mobile)<source media="(max-width: 640px)" srcset="{{ $mobile->url() }}">@endif@if($desktop)<img src="{{ $desktop->url() }}" alt="{{ $desktop->alt_text }}" width="1536" height="1024" fetchpriority="high">@endif</picture><div class="hero-overlay"><p class="eyebrow">{{ data_get($brand->theme_settings,'eyebrow') }}</p><h1>{{ data_get($brand->theme_settings,'hero_title') }}</h1><p>{{ data_get($brand->theme_settings,'hero_note') }}</p><a class="button light" href="{{ route('services.index') }}">探索服務</a></div></section>
<section class="intro section"><p class="eyebrow">OUR APPROACH</p><h2>{{ $page->excerpt }}</h2><p>{{ $page->body }}</p><a class="text-link" href="{{ route('about') }}">認識我們 →</a></section>
<section class="section"><div class="section-head"><div><p class="eyebrow">SERVICES</p><h2>讓綠意住得更久</h2></div><a href="{{ route('services.index') }}">查看全部</a></div><div class="cards">@forelse($services as $item)<article class="card"><x-media-image :media="$item->primaryMedia('cover')"/><div><p class="number">0{{ $loop->iteration }}</p><h3><a href="{{ route('services.show',$item->slug) }}">{{ $item->name }}</a></h3><p>{{ $item->summary }}</p></div></article>@empty<p class="empty">服務內容準備中，歡迎稍後再訪。</p>@endforelse</div></section>
<section class="section course-block"><div class="section-head"><div><p class="eyebrow">WORKSHOPS</p><h2>和植物相處的練習</h2></div><a href="{{ route('courses.index') }}">查看全部</a></div><div class="cards">@forelse($courses as $item)<article class="card"><x-media-image :media="$item->primaryMedia('cover')"/><div><p class="meta">{{ $item->duration_minutes }} 分鐘</p><h3><a href="{{ route('courses.show',$item->slug) }}">{{ $item->name }}</a></h3><p>{{ $item->summary }}</p></div></article>@empty<p class="empty">課程內容準備中。</p>@endforelse</div></section>
<section class="contact-strip"><p class="eyebrow">STAY CONNECTED</p><h2>在社群上，繼續分享植物的日常</h2><div>@foreach($links as $link)<a class="button" href="{{ $link->url }}" target="_blank" rel="noopener noreferrer">{{ $link->label }} ↗</a>@endforeach</div></section>@endsection
