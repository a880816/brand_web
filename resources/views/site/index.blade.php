@extends('layouts.site') @section('title',$kind.'｜'.$brand->name) @section('content')<section class="page-hero"><p class="eyebrow">{{ $kind==='服務'?'SERVICES':'COURSES' }}</p><h1>{{ $kind }}</h1><p>{{ $kind==='服務'?'從空間理解開始，提供可以長久維持的植物提案。':'從親手觸碰開始，理解植物真正需要的照顧。' }}</p></section><section class="section"><div class="cards listing">@forelse($items as $item)<article class="card"><x-media-image :media="$item->primaryMedia('cover')"/><div><p class="number">{{ str_pad($loop->iteration,2,'0',STR_PAD_LEFT) }}</p><h2><a href="{{ route($route,$item->slug) }}">{{ $item->name }}</a></h2><p>{{ $item->summary }}</p><a class="text-link" href="{{ route($route,$item->slug) }}">了解更多 →</a></div></article>@empty<p class="empty">目前尚無已發布的{{ $kind }}。</p>@endforelse</div></section>@endsection
