@extends('layouts.site') @section('title',($item->seo_title ?: $item->name).'｜'.$brand->name) @section('content')<section class="detail-hero"><x-media-image :media="$item->primaryMedia('cover')" :eager="true" ratio="16/8"/><div><p class="eyebrow">{{ $kind==='服務'?'SERVICE':'COURSE' }}</p><h1>{{ $item->name }}</h1><p>{{ $item->summary }}</p></div></section><section class="detail-grid section"><div class="prose">@foreach(preg_split('/\n\n+/', $item->description) as $p)<p>{{ $p }}</p>@endforeach</div>@if($kind==='課程')<aside class="facts"><h2>課程資訊</h2><dl><dt>時間</dt><dd>{{ $item->duration_minutes }} 分鐘</dd><dt>地點</dt><dd>{{ $item->location_note }}</dd><dt>參考費用</dt><dd>NT$ {{ number_format((float)$item->price_amount) }}<br><small>{{ $item->price_note }}</small></dd></dl></aside>@endif</section><section class="section"><x-gallery :media="$item->mediaFor('gallery')->get()" /></section>@if($kind==='課程')<section class="contact-strip"><h2>想了解課程或報名方式？</h2><p>請透過品牌社群與我們聯絡，我們會提供最新課程資訊。</p><div>@foreach($links as $link)<a class="button" href="{{ $link->url }}" target="_blank" rel="noopener noreferrer">透過 {{ $link->label }} 聯絡 ↗</a>@endforeach</div></section>@endif<section class="section"><a class="text-link" href="{{ route($back) }}">← 返回{{ $kind }}列表</a></section>@endsection
